type Query {
  users: [User]
  notes: [Note]
  chats: [Chat]
  chat(id: String!): Chat
  chatCompletion(input: ChatCompletionInput!): ChatCompletion
  user(id: String!): User
  usersNotes(userId: String!): [Note]
  usersChats(userId: String!): [Chat]
}
