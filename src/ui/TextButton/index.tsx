import { styled } from "../stitches.config";

export const TextButton = styled("button", {
  background: "none",
  color: "inherit",
  border: "none",
  padding: "0.5rem",
  cursor: "pointer",
  fontSize: "$1",
});
